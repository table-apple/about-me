<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="name_style">
        <item name="android:layout_gravity">center_horizontal</item>
        <item name="android:layout_marginHorizontal">@dimen/text_view_margin_small</item>
        <item name="android:layout_marginVertical">@dimen/text_view_margin_small</item>
        <item name="android:fontFamily">@font/roboto</item>
        <item name="android:paddingHorizontal">@dimen/text_view_padding_small</item>
        <item name="android:paddingVertical">@dimen/text_view_padding_small</item>
        <item name="android:textAppearance">@style/TextAppearance.AppCompat.Large</item>
        <item name="android:textColor">@android:color/black</item>
    </style>

</resources>
